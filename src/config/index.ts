export const ENV_USER_STORAGE_KEY = String(
  import.meta.env.VITE_USER_STORAGE_KEY,
);
export const ENV_USER = String(import.meta.env.VITE_USER);
export const ENV_PASSWORD = String(import.meta.env.VITE_PASSWORD);
